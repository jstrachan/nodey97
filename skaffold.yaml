apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/jstrachan/nodey97:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/jstrachan/nodey97:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: nodey97
        chartPath: charts/nodey97
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/jstrachan/nodey97'
          image.tag: '{{.DIGEST_HEX}}'
